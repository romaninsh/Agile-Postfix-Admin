user            = mailsql
password        = secret
dbname          = mailsql
table           = users
select_field    = relocated_to
where_field     = email
additional_conditions = and postfix like 'y' and relocated='Y'
#hosts           = unix:/var/run/mysqld/mysqld.sock
hosts           = mysql.adevel.com
